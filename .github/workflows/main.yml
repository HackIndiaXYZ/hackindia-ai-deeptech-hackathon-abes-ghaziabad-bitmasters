name: Deploy to Render

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Render via API
        run: |
          curl -X POST https://api.render.com/v1/services/srv-d81pdereo5us73afkbbg/deploys \
               -H "Authorization: Bearer rnd_mxE08ckiUIVl7xvsvWivcArPLeeq" \
               -H "Accept: application/json"
